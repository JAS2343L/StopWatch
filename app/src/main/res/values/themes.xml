<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StopWatch" parent="Theme.Material3Expressive.DynamicColors.DayNight.NoActionBar">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <!-- Ensure status bar icons (clock, battery) stay white for contrast -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>

    <style name="Theme.StopWatch" parent="Base.Theme.StopWatch" />
</resources>